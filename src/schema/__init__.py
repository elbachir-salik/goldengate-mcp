"""
Schema mapping layer.

    mapper         — loads schema_map.yaml; resolves logical entity/column names
                     to physical Oracle table/column names at runtime
    schema_map.yaml — bank-specific mapping config (the ONLY place physical names live)
"""
