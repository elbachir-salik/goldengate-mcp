"""
Write-back layer — generic REST client for the bank's external endpoint.

The server has no knowledge of what system is behind the endpoint.
URL and auth are configured via WRITEBACK_BASE_URL and WRITEBACK_API_KEY.
"""
